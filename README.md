# Game-Template

This is a template repo for quickly setting up base architecture for creating software or games,

The repo contains git submodules under skz.

Currently:
* skz-stride - render api


when cloning use:
git clone --recurse-submodules <repo>
